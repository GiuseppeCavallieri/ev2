server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/GiuseppeCavallieri/ev2
          search-paths: backend/config-data
  application:
    name: config-service
